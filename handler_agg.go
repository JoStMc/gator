package main

/*
import (
	"context"
	"fmt"
)
*/

func handlerAgg(s *state, cmd command) error {
	/* feed, err := fetchFeed(context.Background(), "")
	if err != nil {
	    return err
	} 
	fmt.Println(feed) */
	return nil
} 

